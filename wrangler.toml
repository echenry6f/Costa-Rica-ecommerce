name = "ticorastore"
compatibility_date = "2024-01-01"
main = "worker.js"

# Custom domain (ticora.store) is configured in Cloudflare dashboard → Worker → Triggers
# Do not add [[routes]] here — it conflicts with existing DNS records

[assets]
directory = "."
binding = "ASSETS"
